package io.ampznetwork.lunararc.common.mixin.core.entity;

/** Retired in Runtime Fix 11: SpectralArrow.duration is access-widened and used directly. */
public final class SpectralArrowAccessor {
    private SpectralArrowAccessor() {}
}
