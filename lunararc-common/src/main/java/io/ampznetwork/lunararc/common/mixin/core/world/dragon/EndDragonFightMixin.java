package io.ampznetwork.lunararc.common.mixin.core.world.dragon;

/**
 * Retired in Runtime Fix 04.
 * EndDragonFight compatibility now uses direct access-widened NMS members from CraftDragonBattle.
 * This inert marker remains so overlay updates cannot leave obsolete shadow/invoker declarations behind.
 */
final class EndDragonFightMixin {
    private EndDragonFightMixin() {
    }
}
