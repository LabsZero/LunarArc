package io.ampznetwork.lunararc.common.mixin.core.network;

/** Retired in Runtime Fix 09. Connection.address is already accessed directly through LunarArc's access widener. */
public final class ConnectionAccessor {
    private ConnectionAccessor() {}
}
