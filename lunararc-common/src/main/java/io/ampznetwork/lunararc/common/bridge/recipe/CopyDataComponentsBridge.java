package io.ampznetwork.lunararc.common.bridge.recipe;

/** Paper smithing state mixed directly into vanilla 1.21.1 recipe objects. */
public interface CopyDataComponentsBridge {
    boolean lunararc$copyDataComponents();
    void lunararc$copyDataComponents(boolean copy);
}
