// buildSrc — custom Gradle build logic for LunarArc.
// Analogous to Arclight's buildSrc (Paper remapping utilities, etc.)
plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    gradlePluginPortal()
    mavenCentral()
}
